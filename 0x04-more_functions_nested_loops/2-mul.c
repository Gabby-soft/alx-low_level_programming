#include "main.h"
/**
 * mul - multipies two integers and returns the result
 *
 * @a: is teh first number
 * @c: is the second number
 *
 * Return: the multiple of @a and @c
 */
int mul(int a, int c)
{
	return (a * c);
}
